import React from 'react';

import Column from '../Column/Column';

import './board.scss';

const Board = () => {
  return (
    <main className="board">
      <Column title="tets" />
    </main>
  );
};

export default Board;
